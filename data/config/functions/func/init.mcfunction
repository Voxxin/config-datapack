## Note:
# This function is only to setup the environment for the first time.
# This isn't the area where you add your own variables / config options.
# To add your own variables / config options, navigate to the 'variables.mcfunction' file.

data merge storage config.data:config {Config:[]}


# If you have trouble finding the file location, you can navigate to it below.
function config:func/variables